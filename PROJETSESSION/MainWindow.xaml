<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PROJETSESSION.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PROJETSESSION">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <MenuBar Grid.Row="0" HorizontalAlignment="Left">

            <MenuBarItem Title="Exporter le fichier" >
                <MenuFlyoutItem Text="Projet.csv" Tag="projets" Click="MenuFlyoutItem_Click"/>
            </MenuBarItem>
        </MenuBar>

        <NavigationView Grid.Row="2" 
                 PaneDisplayMode="Left" 
                 PaneTitle="PROJETS DE SESSION" 
                 x:Name="navView" 
                 ItemInvoked="navView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Gestion des employés" x:Name="iEmploye" Icon="List"  Tag="afficherEmploye"/>
                <NavigationViewItem Content="Ajouter des employés" x:Name="iEmployeAjout" Icon="Add"  Tag="ajouterEmploye"/>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Gestions des clients" x:Name="iClients" Icon="List" Tag="afficherClients"/>
                <NavigationViewItem Content="Ajouter des clients" x:Name="iClientsAjout" Icon="Add" Tag="ajouterClients"/>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Gestions des projets" x:Name="iProjets" Icon="List" Tag="afficherProjets"/>
                <NavigationViewItem Content="Ajouter des projets" x:Name="iProjetsAjout" Icon="Add" Tag="AjouterProjets"/>
            </NavigationView.MenuItems>

            <Frame x:Name="mainFrame"/>
        </NavigationView>

    </Grid>
</Window>
