<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROJETSESSION.Pages.ClientPages.PageAfficherClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION.Pages.ClientPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:classes="using:PROJETSESSION.Classes">

    <Grid Background="Beige">
       
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>







        <TextBlock Text="LISTE DE TOUS LES CLIENTS"  Foreground="Green" TextAlignment="Center" FontSize="32" Margin="20"/>
            <GridView x:Name="lvListeClients" Grid.Row="1">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="classes:Clients">
                    <StackPanel   Margin="20" Spacing="8">

                            <TextBlock x:Name="tbxIdentifiant" Text="{x:Bind identifiant, Mode=OneWay}" FontWeight="Bold" Foreground="Green" TextAlignment="Center" />
                            <TextBlock Text="{x:Bind nom}" TextAlignment="Center"/>
                            <TextBlock Text="{x:Bind adresse}" TextAlignment="Center"/>
                            <TextBlock Text="{x:Bind numeroTelephone}" TextAlignment="Center"/>
                            <TextBlock Text="{x:Bind email}" TextAlignment="Center"/>

                        <StackPanel x:Name="spBoutons" Orientation="Vertical" Spacing="10">

                                <Button x:Name="btnSupprimer" Click="btnSupprimer_Click">
                                    <StackPanel  Orientation="Horizontal" Spacing="5">
                                        <SymbolIcon Symbol="Delete"/>
                                        <TextBlock Text="Supprimer"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="btnModifier" Click="btnModifier_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="5">
                                        <SymbolIcon Symbol="Edit"/>
                                        <TextBlock Text="Modifier"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                    </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>



    </Grid>
    

</Page>
