<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROJETSESSION.Pages.EmployePages.AfficherEmployes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION.Pages.EmployePages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:classes="using:PROJETSESSION.Classes">
    


    <Grid Background="Beige">
        <Grid.RowDefinitions>
        <RowDefinition Height="auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>


        
    <TextBlock x:Name="tbltexte" TextAlignment="Center" Text="LISTE DES EMPLOYÉS"  FontSize="32" Margin="20"/>
        <GridView x:Name="lvListeEmploye" Grid.Row="1">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="classes:Employes">
                    <StackPanel  Margin="20">

                        <Image Source="{x:Bind Photo}" Width="125" />
                        
                        <TextBlock x:Name="tbxCode" Text="{x:Bind Matricule, Mode=OneWay}" Foreground="Green" TextAlignment="Center" />
                        <TextBlock Text="{x:Bind Nom}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Prenom}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind DateNaissanceString, Mode=OneWay}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Email}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind DateEmbaucheString}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind TauxHoraires}" TextAlignment="Center"/>
                        
                        <TextBlock Text="{x:Bind Statut}" Foreground="Green" TextAlignment="Center"/>
                        <StackPanel Orientation="Horizontal">
                             <Button x:Name="btnSupprimer" Click="btnSupprimer_Click" >
                             <StackPanel Orientation="Horizontal" Spacing="20">
                                 <SymbolIcon Symbol="Delete"/>
                             </StackPanel>
                             </Button>
                             <Button x:Name="btnModifier" Click="btnModifier_Click" >
                                 <StackPanel Orientation="Horizontal" Spacing="20">
                                     <SymbolIcon Symbol="Edit"/>
                                 </StackPanel>
                             </Button>
                        </StackPanel>
                       
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        

        </Grid>
</Page>
