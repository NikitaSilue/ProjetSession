<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROJETSESSION.Pages.ProjetPages.PageAfficherProjets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION.Pages.ProjetPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:classes="using:PROJETSESSION.Classes">

    <Grid Background="Beige" >

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>



        <TextBlock x:Name="tbltexte" TextAlignment="Center" Text="LISTE DE TOUS LES PROJETS" Foreground="Green"  FontSize="32" Margin="20"/>
        
        <GridView x:Name="lvListeProjet" Grid.Row="1"  IsItemClickEnabled="True" ItemClick="lvListeProjet_ItemClick" >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="classes:Projets">
                    <StackPanel  Margin="20">

                        
                            <TextBlock x:Name="tbxCode" Text="{x:Bind NoProjet, Mode=OneWay}" Foreground="Green" TextAlignment="Center" Margin="5" />
                        <TextBlock Text="{x:Bind Titre}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind NoClient}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind DateDebutString, Mode=OneWay}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Budjet}" TextAlignment="Center"/>
                        <TextBlock Text="{x:Bind Statut}" Foreground="Green" TextAlignment="Center"/>

                        <StackPanel x:Name="spBoutons" Orientation="Horizontal" Spacing="15" HorizontalAlignment="Center" Margin="0,5,0,0">
                            <Button x:Name="btnSupprimer" Click="btnSupprimer_Click" >
                                <StackPanel Orientation="Horizontal" Spacing="20">
                                    <SymbolIcon Symbol="Delete"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="btnModifier" Click="btnModifier_Click" >
                                <StackPanel Orientation="Horizontal" Spacing="20">
                                    <SymbolIcon Symbol="Edit"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
            
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        


    </Grid>
</Page>
