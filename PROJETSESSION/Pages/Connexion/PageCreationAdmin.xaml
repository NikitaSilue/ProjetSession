<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROJETSESSION.Pages.Connexion.PageCreationAdmin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION.Pages.Connexion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Beige">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="500">
            <Border Background="White" Padding="40" CornerRadius="10" Margin="0,0,0,10">
                <StackPanel Spacing="20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20">

                        <TextBlock Text="PREMIER UTILISATION" FontWeight="Bold" Foreground="Green" FontSize="32" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    </StackPanel>


                    <TextBlock Text="Création du compte admin" HorizontalAlignment="Center" FontWeight="Bold" Foreground="Green" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>


                    <TextBox x:Name="tbxNomUtilisateur" Header="Nom d'utilisateur :"/>
                    <TextBlock x:Name="erreurNomUtilisateur" Foreground="Red"/>


                    <PasswordBox x:Name="pbxMotDePasse" Header="Mot de passe" />
                    <TextBlock x:Name="erreurMotDePasse" Foreground="red"/>

                    <PasswordBox x:Name="pbxConfirmation" Header="Confirmer le mot de passe: "/>
                    <TextBlock x:Name="erreurConfirmation" Foreground="Red"/>

                    <Button x:Name="btnCree" Click="btnCree_Click" Background="Green" Foreground="White" HorizontalAlignment="Center" >
                        <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                            <SymbolIcon Symbol="Contact"/>
                            <TextBlock Text="Créer"/>
                        </StackPanel>
                    </Button>

                   
                </StackPanel>
            </Border>
        </StackPanel>

    </Grid>
</Page>
