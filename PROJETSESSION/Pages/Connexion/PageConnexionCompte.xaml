<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PROJETSESSION.Pages.Connexion.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PROJETSESSION.Pages.Connexion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Beige">

        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <Border Background="White" Padding="40" CornerRadius="10" Margin="0,0,0,10">
                <StackPanel Spacing="20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20">

                        <TextBlock Text="CONNECTEZ-VOUS" FontWeight="Bold" Foreground="Green" FontSize="32" VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <SymbolIcon Symbol="Contact" Foreground="Green" VerticalAlignment="Center">
                            <SymbolIcon.RenderTransform>
                                <ScaleTransform ScaleX="2" ScaleY="2"/>
                            </SymbolIcon.RenderTransform>
                        </SymbolIcon>

                    </StackPanel>



                    <TextBox x:Name="tbxNomUtilisateur" Header="Nom d'utilisateur :"/>
                    <TextBlock x:Name="erreurNomUtilisateur" Foreground="Red"/>
                    
                    
                    <PasswordBox x:Name="pbxMotDePasse" Header="Mot de passe" />
                    <TextBlock x:Name="erreurMotDePasse" Foreground="red"/>

                    <Button x:Name="btnConnecter" Background="Green" Foreground="White" HorizontalAlignment="Center" Click="btnConnecter_Click">
                        <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                            <SymbolIcon Symbol="Contact"/>
                            <TextBlock Text="se connecter"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="btnAnnuler" Click="btnAnnuler_Click" HorizontalAlignment="Center">
                        Annuler
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>





    </Grid>
</Page>
